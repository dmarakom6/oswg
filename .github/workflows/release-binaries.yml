name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-x86_64
            artifact_name: oswg-linux-x86_64
            binary_name: oswg
          - os: macos-latest
            target: macos-x86_64
            artifact_name: oswg-macos-x86_64
            binary_name: oswg
          - os: macos-latest
            target: macos-arm64
            artifact_name: oswg-macos-arm64
            binary_name: oswg

    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          python build_binary.py frontend
          pip install .
          python build_binary.py binary

      - name: Rename binary for platform
        run: mv dist/${{ matrix.binary_name }} dist/${{ matrix.binary_name }}-${{ matrix.target }}

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.binary_name }}-${{ matrix.target }}

  publish-pypi:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build frontend into package
        run: |
          python -m pip install --upgrade pip
          pip install build
          python build_binary.py frontend

      - name: Build sdist and wheel
        run: |
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          attestations: false
          password: ${{ secrets.PYPI_API_KEY }}
          packages-dir: dist/
          skip-existing: true

  release:
    name: Create GitHub Release
    needs: [build, publish-pypi]
    if: success() || failure()
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List downloaded artifacts
        run: find artifacts/ -type f -ls

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/oswg-linux-x86_64/oswg-linux-x86_64
            artifacts/oswg-macos-x86_64/oswg-macos-x86_64
            artifacts/oswg-macos-arm64/oswg-macos-arm64
          generate_release_notes: true
          draft: false
          prerelease: false
